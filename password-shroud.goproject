/* File generated by Qt Creator (with Ubuntu Plugin), version 3.0.1 */

import GoProject 1.1

GoProject {
    Application {
        name: "password-shroud"

        GoFiles {
        }

        /* Include .qml, .js, and image files from main directory and subdirectories */
        QmlFiles {
            directory:"share/password-shroud"
        }
        JavaScriptFiles {
            directory:"share/password-shroud"
        }
        Files {
            directory: "."
            paths: ["manifest.json","password-shroud.apparmor"]
        }
    }
    Package {
        name: "gopkg.in/qml.v1"
        Files {
            filter: "*.*"
        }
    }
}

